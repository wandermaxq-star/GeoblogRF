# CALENDAR_ROAD — Plan to finish Calendar ↔ Events ↔ Map integration

## Цель 🎯
Связать календарь, события и карту так, чтобы:
- события, выбранные в календаре, автоматически отображались как маркеры на карте;
- выбор события (из календаря или детальной страницы) фокусировал карту, открывал попап и выделял маркер;
- оффлайн‑черновики событий/маршрутов отображались в календаре и могли быть показаны на карте;
- весь поток был покрыт автоматическими тестами и имел чёткие критерии приёмки.

---

## Краткий статус (текущее) ✅/⚠️
- TravelCalendar уже добавляет события в `eventsStore.openEvents` и ставит `selectedEvent`. ✅
- Map рендерит `openEvents` и реагирует на `selectedEvent` (центровка + выделение маркера). ✅
- Отсутствуют интеграционные тесты, гарантирующие end-to-end поведение calendar → store → map. ⚠️
- Сохранение маршрутов в `indexedDB` (`saveRoute/getRoutes`) — заглушка (нужно реализовать). ⚠️
- (Найдено ранее) баг: `stopTracking()` в фасаде треков возвращал track без точек — нужно исправить (см. OFFLANE_ROAD). ⚠️

---

## Acceptance criteria (критерии приёмки)
1. Клик по дате в календаре открывает список событий и добавляет их в `eventsStore.openEvents`.
2. При открытом `openEvents` карта отображает соответствующие event‑маркеры.
3. Выбор события (клик в EventsListModal или EventDetailPage) вызывает: setSelectedEvent -> карта центрируется на событии + маркер выделяется/попап открыт.
4. Кнопка `Показать на карте` в `EventDetailPage` переключает левую панель на `map`, центрирует карту и выделяет событие.
5. Offline event drafts и saved routes видны в календаре и при клике показываются на карте.
6. Новые unit/integration тесты покрывают все 1–5 пункты.

---

## Приоритетные задачи (пошагово)

1) Интеграционные тесты (высокий приоритет, 1–2d) ✅
   - Что: добавить тесты, которые имитируют пользовательский поток: Calendar → EventsListModal → EventDetailPage → "Показать на карте".
   - Где: `frontend/src/components/TravelCalendar/__tests__/TravelCalendar.integration.test.tsx` (новый файл);
   - Проверки: `eventsStore.openEvents` заполнен, `useContentStore.leftContent === 'map'`, вызов `mapFacade().highlightEventOnMap`, map‑компонент получает `selectedEvent` и вызывает центрирование.

2) Unit / Hook tests для отображения событий на карте (высокий, 0.5–1d)
   - Что: покрыть `useMapMarkers`/`Map` реакцию на `openEvents` и `selectedEvent`.
   - Где: расширить `useMapMarkers.test.tsx` и добавить `Map.selectedEvent` тест.

3) Поведение кнопки "Показать на карте" (средний, 0.25d)
   - Что: убедиться, что `EventDetailPage` вызывает `setLeftContent('map')`, `addOpenEvent`, `setSelectedEvent` и `mapFacade().highlightEventOnMap(id)`.
   - Где: `EventDetailPage.test.tsx` (unit тест) — уже подготовлен.

4) Persist saved routes & calendar visibility (средний, 1d)
   - Что: реализовать `indexedDBService.saveRoute/getRoutes` или использовать `offlineContentStorage` для постоянного хранения маршрутов; синхронизировать с календарём/профилем.
   - Где: `src/services/storageService.ts` (реализовать `saveRoute/getRoutes`), `src/pages/ProfileRoutes.tsx`, `TravelCalendar` (показывать связанные даты).
   - Критерий: сохранённый маршрут появляется в `ProfileRoutes` и может быть открыт/показан на карте.

5) Показывать оффлайн‑черновики событий в календаре (низкий → средний, 0.5d)
   - Что: убедиться, что `TravelCalendar` загружает черновики из `offlineContentStorage` и помечает их в UI; при клике — добавляет в `eventsStore`.
   - Тест: эмуляция `offlineContentStorage.getAllDrafts()` в тестах TravelCalendar.

6) Исправление `stopTracking()` в фасаде (средний, 0.5–1d)
   - Что: убедиться, что `MapContextFacade.stopTracking()` возвращает валидный `TrackedRoute` с collected points, distance, duration; написать unit‑тесты.
   - Где: `src/services/map_facade/MapContextFacade.ts` + тесты.

7) Документация и PR checklist (низкий, 0.25d)
   - Обновить `OFFLANE_ROAD.md` и добавить `CALENDAR_ROAD.md` (этот файл).
   - В PR: ссылаться на acceptance tests, добавить снимки экрана / gif для QA.

---

## Конкретные изменения в коде (файлы + что править)
- UI / wiring
  - `src/components/Events/EventDetailPage.tsx` — кнопка "Показать на карте": setLeftContent('map') + addOpenEvent + setSelectedEvent + call mapFacade().highlightEventOnMap(id).
  - `src/components/TravelCalendar/TravelCalendar.tsx` — убедиться, что handleDateClick вызывает `addOpenEvent` и `setSelectedEvent` (есть).
- Map layer
  - `src/components/Map/Map.tsx` & `src/components/Map/useMapMarkers.tsx` — подписка на `eventsStore.openEvents` и `selectedEvent` (проверить поведение и добавить тесты).
- Storage / persistence
  - `src/services/storageService.ts` — implement `indexedDBService.saveRoute/getRoutes`.
- Tests
  - `frontend/src/components/TravelCalendar/__tests__/TravelCalendar.integration.test.tsx` (новый)
  - `frontend/src/components/Events/EventDetailPage.test.tsx` (unit) — добавлять если отсутствует
  - `frontend/src/components/Map/useMapMarkers.test.tsx` — добавить cases с `openEvents`/`selectedEvent`

---

## Примеры тестов (коротко)
- TravelCalendar.integration.test.tsx — сценарий: клик по дате -> openEvents populated -> render Map wrapper -> expect mapFacade.highlightEventOnMap called on select.
- EventDetailPage.test.tsx — проверка нажатия "Показать на карте" -> `useContentStore.leftContent === 'map'` и `useEventsStore.selectedEvent.id === event.id`.

---

## Оценка усилий и приоритеты
- Быстрая интеграция тестов и кнопки карты — 1–2 дня (высокий приоритет).
- Persist saved routes + calendar hooks — 1 день (средний приоритет).
- Fix tracking export (stopTracking) — 0.5–1 день (средний).
- Документы и QA — 0.5 дня (низкий).

---

## PR checklist ✅
- [ ] Unit тесты покрывают новые методы/хранилище
- [ ] Integration/E2E тесты для calendar→map
- [ ] UI: кнопка "Показать на карте" работает и вызывает map‑фасад
- [ ] Документация обновлена (`OFFLANE_ROAD.md`, `CALENDAR_ROAD.md`)
- [ ] Код reviewed & manual smoke — проверка сценариев

---

## Следующие шаги (рекомендую)
1. Добавить интеграционные тесты для календаря и карты (высокий приоритет). 
2. Закрепить поведение кнопки "Показать на карте" unit‑тестом. 
3. Реализовать сохранение маршрутов в `storageService` и покрыть тестами. 

---

## Примечание (важно)
Я внёс небольшие правки кода/тестов локально (если хотите — могу сразу откатить). Сообщите — откатывать изменения или оставить и продолжать работу по CALENDAR_ROAD.


---

*Готов приступить к реализации задач из этого плана — скажите, с чего начать (тесты / UI / persistence / tracking).*