# 🎨 Глобальные стили: Light / Dark Glass Theme

## Эталон: Контролы карты (MapActionButtons — `.btn-glass`)

Именно этот стиль нравится и должен быть применён **ко всему проекту**.

---

## 🔑 CSS-переменные (будут в `_glass-theme.css`)

> **Важно:** в пользовательском интерфейсе теперь всего две **логические
> темы**: «primary» (иконка ☀️) и «emerald» (иконка 🌙). При этом primary
> на картах отображается как **светлая**, а на solo‑страницах — как **тёмная**.
> Emerald остаётся зелёной везде. Фактические значения `data-theme` могут
> быть `light`, `dark` или `emerald`, но для разработчиков достаточно помнить
> про две кнопки.


### LIGHT (по умолчанию)

| Переменная | Значение | Описание |
|---|---|---|
| `--glass-bg` | `rgba(255, 255, 255, 0.1)` | Фон панелей/контейнеров |
| `--glass-bg-hover` | `rgba(255, 255, 255, 0.15)` | Фон при hover |
| `--glass-bg-active` | `rgba(255, 255, 255, 0.2)` | Фон активного элемента |
| `--glass-card-bg` | `rgba(255, 255, 255, 0.15)` | Фон карточек/элементов |
| `--glass-card-bg-hover` | `rgba(255, 255, 255, 0.22)` | Фон карточки при hover |
| `--glass-blur` | `blur(10px) saturate(180%)` | Backdrop-filter |
| `--glass-blur-strong` | `blur(16px) saturate(180%)` | Усиленный blur (sidebar, header) |
| `--glass-border` | `1px solid rgba(255, 255, 255, 0.2)` | Рамка |
| `--glass-border-hover` | `1px solid rgba(255, 255, 255, 0.3)` | Рамка при hover |
| `--glass-shadow` | `0 4px 12px rgba(0, 0, 0, 0.15)` | Тень |
| `--glass-shadow-hover` | `0 6px 20px rgba(0, 0, 0, 0.2)` | Тень при hover |
| `--glass-shadow-container` | `0 8px 32px rgba(0, 0, 0, 0.1)` | Тень контейнера |
| `--glass-text` | `rgba(0, 0, 0, 0.8)` | Основной текст |
| `--glass-text-secondary` | `rgba(0, 0, 0, 0.5)` | Вторичный текст |
| `--glass-text-muted` | `rgba(0, 0, 0, 0.35)` | Приглушённый текст |
| `--glass-radius` | `20px` | Скругление контейнеров |
| `--glass-radius-sm` | `12px` | Скругление кнопок/карточек |
| `--glass-radius-xs` | `8px` | Скругление мелких элементов |

### DARK

| Переменная | Значение | Описание |
|---|---|---|
| `--glass-bg` | `rgba(0, 0, 0, 0.2)` | Фон панелей — полупрозрачный тёмный |
| `--glass-bg-hover` | `rgba(0, 0, 0, 0.25)` | Фон при hover |
| `--glass-bg-active` | `rgba(0, 0, 0, 0.3)` | Фон активного элемента |
| `--glass-card-bg` | `rgba(255, 255, 255, 0.06)` | Фон карточек — чуть светлее фона |
| `--glass-card-bg-hover` | `rgba(255, 255, 255, 0.10)` | Фон карточки при hover |
| `--glass-blur` | `blur(10px) saturate(180%)` | Backdrop-filter (тот же) |
| `--glass-blur-strong` | `blur(16px) saturate(180%)` | Усиленный blur |
| `--glass-border` | `1px solid rgba(255, 255, 255, 0.1)` | Рамка — более тонкая |
| `--glass-border-hover` | `1px solid rgba(255, 255, 255, 0.2)` | Рамка при hover |
| `--glass-shadow` | `0 4px 12px rgba(0, 0, 0, 0.25)` | Тень — чуть глубже |
| `--glass-shadow-hover` | `0 6px 20px rgba(0, 0, 0, 0.3)` | Тень при hover |
| `--glass-shadow-container` | `0 8px 32px rgba(0, 0, 0, 0.2)` | Тень контейнера |
| `--glass-text` | `rgba(255, 255, 255, 0.9)` | Основной текст — белый |
| `--glass-text-secondary` | `rgba(255, 255, 255, 0.6)` | Вторичный текст |
| `--glass-text-muted` | `rgba(255, 255, 255, 0.4)` | Приглушённый текст |
| `--glass-radius` | `20px` | Скругление — то же |
| `--glass-radius-sm` | `12px` | Скругление — то же |
| `--glass-radius-xs` | `8px` | Скругление — то же |

---

## 📋 Инвентаризация компонентов

### ✅ Соответствует эталону (не трогаем)
| Компонент | Файл | Стиль |
|---|---|---|
| Контролы карты (`.btn-glass`) | `_components.css` L832 | `rgba(255,255,255,0.1)` + `blur(10px)` ✓ |
| Контейнер контролов | `_components.css` L999 | `rgba(255,255,255,0.1)` + `blur(10px)` ✓ |

### 🔶 Близко, нужна подстройка
| Компонент | Файл | Текущий стиль | Что исправить |
|---|---|---|---|
| Левый сайдбар | `Sidebar.tsx` L331 | `rgba(255,255,255,0.15)` + `blur(16px)` | Почти ОК, чуть ярче эталона |
| Верхний хедер (Topbar) | `Topbar.tsx` L37 | `rgba(255,255,255,0.15)` + `blur(16px)` | Почти ОК, чуть ярче эталона |

### ❌ Не соответствует — требует исправления
| Компонент | Файл | Текущий стиль | Проблема |
|---|---|---|---|
| Панель категорий | `CategoryQuickFilter.tsx` L119,223 | `rgba(25,25,30,0.75-0.85)` | Чёрная, непрозрачная |
| Тулбар региона (на карте) | `Map.tsx` L1103 | `rgba(30,30,35,0.6)` | Тёмная полоса |
| RegionSelector кнопка | `RegionSelector.css` L7 | `rgba(255,255,255,0.1)` | Сама ОК, но контейнер тёмный |
| Карточки постов | `PostCard.tsx` L42 | `rgba(255,255,255,0.72)` | Слишком белые/непрозрачные |
| Панель постов (dark) | `_content.css` L309 | `rgba(255,255,255,0.06)` | ОК для dark, но light не эталон |
| Форма добавления метки | ? | Отдельный стиль | Не в эталоне |
| Попап маркера | ? | Свой стиль | Не в эталоне |

---

## 🎛 Переключатель темы

**Расположение:** Topbar, слева от аватара пользователя  
**Вид:** Иконка солнце ☀️ / луна 🌙 (как на скрине — переключатель day/night)  
**Файл:** `Topbar.tsx`, вставить перед `{isGuest ? (`  
**Хранение состояния:** `localStorage` + zustand store (глобально)  
**CSS-класс:** Добавляется на `<body>` или корневой `<div>` — `data-theme="light"` / `data-theme="dark"`

---

## 📁 Файловая структура

```
frontend/src/styles/
├── _glass-theme.css      ← НОВЫЙ: CSS-переменные light/dark
├── _content.css           ← Рефакторинг: заменить хардкод на var()
├── _components.css        ← Без изменений (эталон)
├── GlobalStyles.css       ← Рефакторинг: убрать конфликты
├── PageLayout.css         ← Рефакторинг: убрать конфликты
└── index.css              ← Добавить @import './_glass-theme.css'
```

---

## 📐 Порядок миграции

1. ✅ Создать `light-dark.md` (этот файл)
2. ⬜ Создать `_glass-theme.css` с CSS-переменными
3. ⬜ Добавить переключатель темы в Topbar
4. ⬜ Создать zustand store для темы (`useThemeStore`)
5. ⬜ Миграция компонентов (по одному):
   - ⬜ Панель категорий (`CategoryQuickFilter.tsx`)
   - ⬜ Тулбар региона на карте (`Map.tsx`)
   - ⬜ Карточки постов (`PostCard.tsx`)
   - ⬜ Панель постов (`_content.css`)
   - ⬜ Форма добавления метки
   - ⬜ Попапы маркеров
   - ⬜ Модалки
6. ⬜ Тестирование light режима
7. ⬜ Тестирование dark режима
