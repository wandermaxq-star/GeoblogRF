# Backend — example .env for development / staging

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=geoblog_user
DB_PASSWORD=secure_password
DB_DATABASE=geoblog

# JWT
JWT_SECRET=change_this_in_production
JWT_EXPIRES_IN=24h

# Server
PORT=3002
NODE_ENV=development

# CORS / Frontend origin
CORS_ORIGIN=http://localhost:5175

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

# WebSocket
WS_PORT=8080
WS_HOST=localhost

# External APIs (set in production!)
# OpenRouteService — required in production for routing features
OPENROUTE_SERVICE_API_KEY=your_openrouteservice_api_key

# SMS provider — required in production for real SMS
SMS_RU_API_ID=your_sms_ru_api_id

# Optional (feature flags / debugging)
# ENABLE_FEATURE_X=false

# NOTES:
# - Do NOT commit real secrets. Use CI / secret manager for production values.
# - In production the server will fail-fast when required keys are missing.
