# ГеоБлог.рф — Backend

![Node.js](https://img.shields.io/badge/Node.js-18+-green)
![Express](https://img.shields.io/badge/Express-4.x-blue)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-13+-blue)
![TypeORM](https://img.shields.io/badge/TypeORM-0.3.x-orange)

Серверная часть проекта **ГеоБлог.рф** — революционной платформы для создания **интерактивных блогов** с интеграцией карт, маршрутов, событий и чата в реальном времени.

## 🚀 Инновационная концепция

**ГеоБлог.рф** — это первая в мире платформа для создания **интерактивных блогов**, где:
- 📝 **Текст** объединяется с **картами** и **маршрутами**
- 🗺️ **Геолокация** становится частью истории
- ⏰ **События** привязываются к времени и месту
- 🎮 **Геймификация** мотивирует пользователей через достижения и уровни
- 🌍 **Сообщество** создает, делится и исследует вместе

## 🛠️ Технологии

### Backend Stack
- **Node.js 18+** - среда выполнения JavaScript
- **Express.js 4.x** - веб-фреймворк для создания API
- **TypeScript** - типизированный JavaScript
- **TypeORM 0.3.x** - ORM для работы с базой данных
- **PostgreSQL 13+** - реляционная база данных
- **JWT** - аутентификация и авторизация
- **bcrypt** - хеширование паролей
- **WebSocket** - реальное время для уведомлений

### Дополнительные библиотеки
- **pg** - драйвер PostgreSQL для Node.js
- **cors** - middleware для CORS
- **helmet** - безопасность HTTP заголовков
- **morgan** - логирование HTTP запросов
- **dotenv** - управление переменными окружения

## 🚀 Быстрый старт

### Предварительные требования
- **Node.js 18+**
- **PostgreSQL 13+**
- **npm** или **yarn**

### Установка и настройка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/simamakis/geoblog.git
   cd geoblog/backend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте базу данных:**
   ```bash
   # Создайте базу данных PostgreSQL
   createdb geoblog
   
   # Или используйте существующую
   ```

4. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл
   ```

5. **Запустите миграции:**
   ```bash
   npm run migration:run
   ```

6. **Запустите сервер:**
   ```bash
   npm run dev
   ```

7. **Проверьте работу:**
   ```
   http://localhost:3002/api/health
   ```

## 📁 Структура проекта

```
backend/
├── src/
│   ├── controllers/        # Контроллеры API
│   │   ├── userController.js         # Управление пользователями
│   │   ├── postController.js         # Управление постами
│   │   ├── activityController.js     # Система активности пользователей
│   │   ├── gamificationController.js # Система геймификации и достижений
│   │   ├── eventController.js        # Управление событиями
│   │   ├── moderationController.js   # Модерация контента
│   │   └── friendsController.js      # Управление друзьями
│   ├── database/          # Конфигурация базы данных
│   │   ├── config/
│   │   │   └── database.config.ts # Конфигурация TypeORM
│   │   └── migrations/    # Миграции базы данных
│   │       ├── 1234567890-CreateUsers.ts
│   │       ├── 1234567891-CreateMarkers.ts
│   │       ├── 1234567892-CreateRoutes.ts
│   │       ├── 1234567893-CreatePosts.ts
│   │       ├── 1234567894-CreateActivities.ts
│   │       ├── 1234567895-CreateGamification.ts
│   │       └── 1234567896-CreateModeration.ts
│   ├── entities/          # Сущности TypeORM
│   │   ├── User.ts              # Модель пользователя
│   │   ├── Post.ts              # Модель поста
│   │   ├── Activity.ts          # Модель активности
│   │   ├── Marker.ts            # Модель метки
│   │   ├── Route.ts             # Модель маршрута
│   │   ├── RouteWaypoint.ts     # Модель точки маршрута
│   │   ├── Achievement.ts       # Модель достижения
│   │   ├── UserLevel.ts         # Модель уровня пользователя
│   │   ├── ModerationLog.ts     # Модель логов модерации
│   │   └── Event.ts             # Модель события
│   ├── middleware/        # Middleware
│   │   ├── auth.js        # Аутентификация JWT
│   │   ├── validation.js  # Валидация данных
│   │   └── zoneGuard.js   # Защита зон
│   ├── routes/              # Маршруты API
│   │   ├── userRoutes.js        # Маршруты пользователей
│   │   ├── postRoutes.js        # Маршруты постов
│   │   ├── activityRoutes.js    # Маршруты активности
│   │   ├── gamificationRoutes.js # Маршруты геймификации
│   │   ├── moderationRoutes.js  # Маршруты модерации
│   │   ├── eventRoutes.js       # Маршруты событий
│   │   └── friendRoutes.js      # Маршруты друзей
│   ├── utils/            # Утилиты
│   │   ├── jwt.js         # JWT утилиты
│   │   ├── password.js    # Хеширование паролей
│   │   └── zoneGuard.js   # Защита зон
│   └── server.js         # Точка входа сервера
├── ws/                   # WebSocket сервер для чата
│   ├── chat_server.php   # PHP WebSocket сервер
│   └── vendor/           # Composer зависимости
├── scripts/              # Скрипты и парсеры
│   ├── events-parser/    # Парсер событий
│   └── wayatom-parser/   # Парсер данных WayAtom
├── logs/                 # Логи приложения
├── public/              # Публичные файлы
│   └── uploads/         # Загруженные файлы
# Конфигурация БД (CommonJS)
├── ormconfig.json       # Конфигурация TypeORM
├── package.json
└── tsconfig.json
```

## 🔧 Конфигурация

### Переменные окружения (.env)
```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_USER=geoblog_user
DB_PASSWORD=secure_password
DB_DATABASE=geoblog

# JWT
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=24h

# Сервер
PORT=3002
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:5175

# Логирование
LOG_LEVEL=info
LOG_FILE=logs/app.log

# WebSocket
WS_PORT=8080
WS_HOST=localhost

# OpenRouteService (required for routing) — set in production
OPENROUTE_SERVICE_API_KEY=your_openrouteservice_api_key

# SMS provider (required in production)
SMS_RU_API_ID=your_sms_ru_api_id
```

> Security notes:
- Removed hard-coded OpenRouteService token from the code — `OPENROUTE_SERVICE_API_KEY` is now required and the server will return 503 when missing.
- `SMS_RU_API_ID` must be provided in production (service will fail fast if missing).

## 📋 Команды

```bash
# Разработка
npm run dev          # Запуск в режиме разработки
npm start            # Запуск production сервера
npm run build        # Сборка TypeScript

# База данных
npm run migration:generate -- -n MigrationName  # Генерация миграции
npm run migration:run        # Запуск миграций
npm run migration:revert     # Откат последней миграции
npm run schema:sync          # Синхронизация схемы

# Тестирование
npm run test         # Запуск тестов
npm run test:watch   # Тесты в режиме наблюдения

# Линтинг
npm run lint         # ESLint проверка
npm run lint:fix     # Автоисправление ESLint

# Утилиты
npm run seed         # Заполнение тестовыми данными
npm run clean        # Очистка временных файлов
```

## 🔌 API Endpoints

### Аутентификация
```
POST   /api/users/register     # Регистрация пользователя
POST   /api/users/login        # Вход в систему
POST   /api/users/logout       # Выход из системы
GET    /api/users/profile      # Получение профиля
PUT    /api/users/profile      # Обновление профиля
```

### Интерактивные посты
```
GET    /api/posts              # Получение всех постов
POST   /api/posts              # Создание поста
GET    /api/posts/:id          # Получение поста по ID
PUT    /api/posts/:id          # Обновление поста
DELETE /api/posts/:id          # Удаление поста
POST   /api/posts/:id/publish  # Публикация поста
POST   /api/posts/:id/draft    # Сохранение как черновик
GET    /api/posts/:id/comments # Получение комментариев
POST   /api/posts/:id/comments # Добавление комментария
```

### Система активности
```
GET    /api/activities         # Получение активности пользователя
POST   /api/activities         # Создание записи активности
GET    /api/activities/stats   # Статистика активности
GET    /api/activities/types   # Типы активности
```

### Маркеры
```
GET    /api/markers            # Получение всех маркеров
POST   /api/markers            # Создание маркера
GET    /api/markers/:id        # Получение маркера по ID
PUT    /api/markers/:id        # Обновление маркера
DELETE /api/markers/:id        # Удаление маркера
```

### Маршруты
```
GET    /api/routes             # Получение всех маршрутов
POST   /api/routes             # Создание маршрута
GET    /api/routes/:id         # Получение маршрута по ID
PUT    /api/routes/:id         # Обновление маршрута
DELETE /api/routes/:id         # Удаление маршрута
```

### Геймификация и достижения
```
GET    /api/gamification/profile         # Профиль геймификации пользователя
GET    /api/gamification/achievements    # Получение всех достижений
GET    /api/gamification/leaderboard     # Таблица лидеров
POST   /api/gamification/daily-check-in  # Ежедневная проверка
GET    /api/gamification/stats           # Статистика пользователя
```

### Модерация
```
GET    /api/moderation/queue             # Очередь модерации
POST   /api/moderation/:id/approve       # Одобрить контент
POST   /api/moderation/:id/reject        # Отклонить контент
GET    /api/moderation/logs              # Логи модерации
POST   /api/moderation/ban-user          # Блокировка пользователя
```

### События
```
GET    /api/events             # Получение всех событий
POST   /api/events             # Создание события
GET    /api/events/:id         # Получение события по ID
PUT    /api/events/:id         # Обновление события
DELETE /api/events/:id         # Удаление события
```

## 🗄️ База данных

### Основные таблицы
- **users** - пользователи системы
- **user_profiles** - профили и настройки пользователей
- **user_levels** - система уровней и XP
- **posts** - интерактивные посты
- **post_comments** - комментарии к постам
- **activities** - система активности пользователей
- **activity_feed** - лента активности с контролем приватности
- **achievements** - достижения и бейджи
- **user_achievements** - прогресс пользователя в достижениях
- **markers** - метки на карте
- **routes** - маршруты
- **route_waypoints** - точки маршрутов
- **events** - события
- **moderation_logs** - логи модерации и контроля качества
- **user_friends** - друзья пользователей

### Инновационные возможности
- **Геолокационная привязка** блогов к местам
- **Временная привязка** событий к блогам
- **Интерактивные элементы** в блогах
- **Трансформация блогов** в книги
- **Система активности** для отслеживания действий

## 🔐 Безопасность

### Аутентификация
- **JWT токены** для аутентификации
- **bcrypt** для хеширования паролей
- **Middleware** для защиты маршрутов
- **Zone Guard** для защиты географических зон

### Валидация
- **Валидация входных данных** на всех endpoints
- **Санитизация** пользовательского ввода
- **Rate limiting** для защиты от DDoS

### CORS
- **Настройка CORS** для фронтенда
- **Белый список** разрешенных доменов

## 📊 Логирование

### Уровни логирования
- **error** - критические ошибки
- **warn** - предупреждения
- **info** - информационные сообщения
- **debug** - отладочная информация

### Файлы логов
- **logs/app.log** - основные логи
- **logs/error.log** - логи ошибок
- **Консоль** - в режиме разработки

## 🧪 Тестирование

### Unit тесты
```bash
npm run test
```

### Интеграционные тесты
```bash
npm run test:integration
```

### Тестовые данные
```bash
npm run seed
```

## 🚀 Развертывание

### Production
```bash
# Сборка
npm run build

# Запуск
npm start

# Или с PM2
pm2 start ecosystem.config.js
```

### Docker
```bash
# Сборка образа
docker build -t geoblog-backend .

# Запуск контейнера
docker run -p 3002:3002 geoblog-backend
```

## 📊 Мониторинг

### Health Check
```
GET /api/health
```

### Метрики
- **Время ответа** API
- **Количество запросов**
- **Ошибки** и их типы
- **Использование памяти**
- **Активность пользователей**

## 🗃️ Archived dev scripts
Чтобы снизить шум в корне `backend` и улучшить читаемость репозитория, одноразовые и тестовые скрипты были перемещены в `archive/fixed_files/backend/`. Эти файлы сохранины в архиве (не удалены) и доступны для восстановления при необходимости.

Перенесённые файлы (пример):
- `testapi.js`, `test_friends_api.js`, `quick-test.js`, `simple-test.js`, `test-api.html`, `test-blog-api.html`, `test-books.html`, `test-events.html`, `test-hashtag-websocket.html`, `test-login.html`
- `websocket-server.js`, `websocket-standalone.js`, `server-simple.js`
- `create-test-user.js`, `create-test-user-with-phone.js`, `create-simple-user.js`
- `1.csv`, `event.csv`, `hashtag-chat-server.js`

> Примечание: инструменты обслуживания и полезные проверки (`check_*.js`, `audit-db.js`, `remove-debug-logs.js`) оставлены в папке `backend/` и помечены как dev-tools.

## 🌟 Уникальные возможности

### Интерактивные посты
- **Геолокационная привязка** контента
- **Интеграция с картами** и маршрутами
- **Временная привязка** событий
- **Интерактивные элементы** (изображения, ссылки, маркеры)
- **Система черновиков** и публикации
- **Редактор постов** с поддержкой медиа
- **Комментарии** и обсуждения

### Система активности и геймификации
- **60+ типов активности** пользователей
- **Система XP и уровней** с математическими формулами
- **Достижения и бейджи** за различные действия
- **Ежедневные цели** и стрик активности
- **Таблица лидеров** для конкуренции
- **Статистика** и аналитика
- **Лента активности** с контролем приватности

### Система модерации
- **AI-фильтрация** контента
- **Панель администратора** для модерации
- **Логирование** всех действий модерации
- **Система блокировки** пользователей
- **Аналитика** нарушений

### Офлайн режим
- **Полная поддержка** создания контента без интернета
- **IndexedDB** для хранения данных
- **Очередь синхронизации** с retry механикой
- **Автоматическая синхронизация** при восстановлении сети

## 🤝 Разработка

### Git Workflow
1. **Создайте** feature ветку
2. **Внесите** изменения
3. **Напишите** тесты
4. **Создайте** Pull Request

### Code Style
- **ESLint** для проверки кода
- **Prettier** для форматирования
- **TypeScript** для типизации

## 📞 Контакты

- **Автор:** Максим Иванов (Simakis)
- **Email:** simakis@gmail.com
- **GitHub:** [@simamakis](https://github.com/simamakis)

---

**ГеоБлог.рф Backend — революционная платформа для интерактивных блогов** 🚀📚✨