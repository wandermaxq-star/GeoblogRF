# OFFLANE_ROAD — Офлайн‑карты: «Карманный геоблог»

> **Концепция:** полноценный гео-блоггинг без интернета — скачанные карты регионов, избранные метки, создание контента в поле с авто-синхронизацией.

---

## 1. Идея и ценность

GeoblogRF — это не GPS-навигатор. Это **карманный дневник путешественника**, который работает без сети.

### Три столпа офлайн-режима:

| Столп | Что делает | Как работает |
|---|---|---|
| **Найти себя** | GPS-точка на скачанной карте | `navigator.geolocation.watchPosition` + тайлы из IndexedDB |
| **Найти интересное** | Избранные (★) метки из постов и рейтингов на карте | Метки кешируются при скачивании региона |
| **Создать контент** | Метка + фото + описание + координата → черновик | Авто-синхронизация при появлении сети |

### Чем это НЕ является:
- ❌ GPS-навигатор с пошаговыми подсказками
- ❌ Фоновая запись трека (background geolocation ненадёжна на iOS)
- ❌ Замена Яндекс.Карт/Google Maps для навигации
- ❌ Скачивание zoom 15-18 (гигабайты, бессмысленно без routing)

---

## 2. Что скачивается при «Скачать регион»

При нажатии кнопки «Скачать» для выбранного региона (Premium-функция):

| Данные | Хранилище | Примерный объём |
|---|---|---|
| Тайлы карты OSM (zoom 6–12) | IndexedDB: `mapTiles` | 5–15 МБ на регион |
| Свои метки в bounds региона | IndexedDB: `userMarkers` | несколько КБ |
| Избранные (★) чужие метки в bounds | IndexedDB: `favoriteMarkers` | несколько КБ |
| Свои маршруты в bounds | IndexedDB: `userRoutes` | десятки КБ |
| Свои посты + превью фото | IndexedDB: `userPosts` | 1–5 МБ |

### Почему zoom 6–12:
- Zoom 12: один тайл ≈ 10×10 км — видны населённые пункты, дороги, реки
- GPS-точность браузера ±5–15 м → пользователь уверенно видит себя на карте
- Zoom 14 = ×16 больше тайлов = 50–150 МБ на регион (избыточно)
- Zoom 12 достаточен для ориентировки: «я на трассе М52 у Чемала»

### Почему только избранные (★), а не «топ-20 популярных»:
- Пользователь **сам курирует** набор интересных мест заранее (онлайн)
- Ставит ★ на метки из постов, рейтингов, рекомендаций друзей
- При скачивании региона все ★-метки в bounds автоматически попадают в IndexedDB
- Реалистично: даже опытный путешественник добавит ≤20 избранных на поездку

---

## 3. Монетизация (Freemium)

| Возможность | Free | Premium |
|---|---|---|
| Видеть карту офлайн (тайлы) | ❌ | ✅ |
| GPS-позиция на карте | ❌ нет карты, GPS работает | ✅ GPS + карта |
| Видеть свои/избранные метки | ❌ | ✅ |
| Создать метку + фото офлайн | ⚠️ «слепое» создание — координата по GPS, карты нет | ✅ полноценно |
| Создать пост/маршрут офлайн | ⚠️ без карты, доредактировать позже | ✅ полноценно |
| Авто-синхронизация | ✅ | ✅ |

**«Слепой режим» (Free):**
- GPS определяет координату точно → метка создаётся с правильным `[lat, lon]`
- Пользователь добавляет фото, текст, категорию
- Но не видит карту → не знает, где стоит относительно дорог/объектов
- При синхронизации: уведомление «Доредактируйте ваши офлайн-метки»

---

## 4. Архитектура

### 4.1. Бэкенд API

```
GET /api/offline/region/:regionId/bundle?userId=...
→ {
    userMarkers: [...],         // метки пользователя в bounds
    favoriteMarkers: [...],     // ★ избранные метки в bounds
    userRoutes: [...],          // маршруты пользователя
    userPosts: [...],           // посты + thumbnail URLs
    tileManifest: {
      baseUrl: "/api/tiles/{z}/{x}/{y}",
      tiles: [{z, x, y}, ...], // конкретные тайлы для региона
      zoomRange: [6, 12],
      totalSize: 12_000_000,   // оценка в байтах
      tileCount: 2400
    }
  }
```

### 4.2. IndexedDB (фронтенд)

```
geoblog_offline (v2)
├── regions          // keyPath: regionId — мета-данные скачанных регионов
├── mapTiles         // keyPath: url — blob тайлов, index: regionId
├── userMarkers      // keyPath: id — метки пользователя, index: regionId
├── favoriteMarkers  // keyPath: id — избранные чужие метки, index: regionId
├── userRoutes       // keyPath: id — маршруты, index: regionId
├── userPosts        // keyPath: id — посты + фото, index: regionId
└── offlineDrafts    // keyPath: id — черновики (создано офлайн)
```

### 4.3. Процесс скачивания

```
1. Пользователь выбирает регион на SVG-карте / в списке
2. Проверка Premium-статуса
3. GET /api/offline/region/:id/bundle
4. Сохранение метаданных в IndexedDB (markers, routes, posts)
5. Скачивание тайлов пачками по 50 штук → IndexedDB (с прогрессом)
6. Статус региона: 'downloaded' → зелёная подсветка на SVG-карте
```

### 4.4. Работа офлайн

```
1. Обнаружение офлайна: navigator.onLine === false
2. Leaflet TileLayer → подменяется на IndexedDB-source
3. Маркеры → рисуются из IndexedDB (свои + ★ избранные)
4. GPS → navigator.geolocation.watchPosition → пульсирующая точка
5. Создание контента → offlineDrafts в IndexedDB
6. При появлении сети → offlineContentQueue отправляет черновики
```

### 4.5. Компоненты фронтенда

```
frontend/src/
├── services/
│   ├── offlineService.ts          // ← расширить: bundle download, tile caching
│   ├── offlineTileLayer.ts        // НОВЫЙ: Leaflet TileLayer из IndexedDB
│   ├── offlineContentStorage.ts   // существует: черновики
│   └── offlineContentQueue.ts     // существует: синхронизация
├── stores/
│   └── offlineTilesStore.ts       // существует: статусы скачивания
├── components/
│   └── Offline/
│       ├── RussiaMapSvg.tsx       // существует: SVG-карта (Albers)
│       ├── RegionPanel.tsx        // существует: список регионов
│       └── OfflineMapViewer.tsx   // НОВЫЙ: офлайн Leaflet-карта
├── pages/
│   ├── OfflinePage.tsx            // существует: страница /offline
│   └── Map.tsx                    // кнопка «Офлайн» с dropdown
└── data/
    ├── russiaRegionsPaths.ts      // SVG-контуры 85 регионов
    └── russiaRegionsGeo.ts        // центры, площади, ФО
```

---

## 5. Избранные метки — жизненный цикл

```
ОНЛАЙН:
  1. Пользователь видит чужую метку в посте / рейтинге / на карте
  2. Нажимает ★ «В избранное» → POST /api/favorites
  3. Метка сохранена на сервере с привязкой к пользователю

СКАЧИВАНИЕ РЕГИОНА:
  4. GET /api/offline/region/:id/bundle → favoriteMarkers[]
  5. Все ★-метки в bounds региона → IndexedDB:favoriteMarkers

ОФЛАЙН:
  6. Открывает офлайн-карту → видит ★-метки на тайлах
  7. Нажимает → popup с описанием, фото, координатами, расстоянием
  8. Может «следовать» — компас / направление (без маршрутизации)
```

---

## 6. Создание контента офлайн

### 6.1. Создание метки

```
1. Нажимает «+» на офлайн-карте
2. GPS определяет координату → [lat, lon]
3. Выбирает категорию, пишет описание, делает фото камерой
4. → offlineDrafts в IndexedDB (type: 'marker')
5. При появлении сети → POST /api/markers → удаление из drafts
```

### 6.2. Создание поста

```
1. Нажимает «Новый пост» (offline mode)
2. Привязывает GPS-координату (или выбирает из избранных меток)
3. Пишет текст, добавляет фото (сжатые, EXIF-координаты)
4. → offlineDrafts в IndexedDB (type: 'post')
5. При появлении сети → POST /api/posts → удаление из drafts
```

### 6.3. Запись маршрута

```
1. Нажимает «Записать маршрут» → watchPosition активен
2. Точки GPS записываются в память каждые N секунд
3. Нажимает «Стоп» → трек сохранён как offlineDraft (type: 'route')
4. При появлении сети → POST /api/routes → удаление из drafts
```

> **Важно:** фоновая запись GPS (background geolocation) НЕ используется.
> Работает только пока приложение открыто в браузере.

---

## 7. UI / точки входа

### Кнопка «Офлайн» на карте (Map.tsx)

Текущая реализация — dropdown с двумя пунктами. Расширяем:

```
[Офлайн ▼]
├── 🗺️  Показать офлайн-слой     // вкл/выкл тайлы из IndexedDB
├── ─────────────────────
├── 📥  Скачать текущий регион    // определяет регион по viewport
├── 📋  Мои скачанные регионы     // список + управление
├── ─────────────────────
└── 🌐  Все регионы (/offline)    // переход на полноэкранную SVG-карту
```

### Страница /offline (OfflinePage.tsx)

Полноэкранная SVG-карта России. Зелёным подсвечены скачанные регионы.
Клик по региону → modal с информацией + кнопкой «Скачать» / «Обновить» / «Удалить».

### Офлайн-режим (автоматический)

Когда `navigator.onLine === false` и есть скачанные тайлы:
- Leaflet автоматически переключается на IndexedDB-тайлы
- Показываются метки из IndexedDB
- Кнопка создания контента доступна
- Индикатор «Офлайн-режим» в углу карты

---

## 8. План реализации (этапы)

### Этап 1: Бэкенд API bundle (1-2 дня)
- [ ] `GET /api/offline/region/:regionId/bundle` — собирает метки, избранные, маршруты, посты
- [ ] `GET /api/offline/region/:regionId/tiles-manifest` — список тайлов для региона
- [ ] Проверка Premium-статуса в middleware

### Этап 2: Расширение offlineService (2-3 дня)
- [ ] Скачивание bundle → IndexedDB (метки, маршруты, посты)
- [ ] Скачивание тайлов пачками с прогрессом → IndexedDB
- [ ] Управление скачанными регионами (обновление, удаление)

### Этап 3: Офлайн-рендеринг карты (2-3 дня)
- [ ] `OfflineTileLayer` — Leaflet TileLayer из IndexedDB
- [ ] Рендеринг маркеров из IndexedDB (свои + ★)
- [ ] Автоматическое переключение онлайн↔офлайн

### Этап 4: GPS на офлайн-карте (1 день)
- [ ] `navigator.geolocation.watchPosition` + пульсирующая точка
- [ ] Кнопка «Центрировать на мне»

### Этап 5: Создание контента офлайн (2-3 дня)
- [ ] Расширить offlineContentStorage для меток/постов с фото
- [ ] UI создания метки/поста в офлайн-режиме
- [ ] Авто-синхронизация при появлении сети

### Этап 6: UI/UX (1-2 дня)
- [ ] Расширить dropdown «Офлайн» в Map.tsx
- [ ] Индикатор офлайн-режима
- [ ] Уведомления о синхронизации
- [ ] «Слепой режим» для Free (GPS без карты)

---

## 9. Существующая инфраструктура

### Уже реализовано:
- ✅ SVG-карта России с 85 регионами (Albers, labels, zoom/pan)
- ✅ Страница `/offline` с SVG-картой + панелью регионов
- ✅ `offlineService.ts` — IndexedDB с region/tile stores
- ✅ `offlineTilesStore.ts` — Zustand-стор со статусами
- ✅ `DownloadRegionModal` — UI скачивания (3 варианта)
- ✅ `offlineContentStorage.ts` — черновики меток/маршрутов/постов
- ✅ `offlineContentQueue.ts` — очередь синхронизации
- ✅ `OfflineOSMRenderer` — адаптер для MBTiles
- ✅ `/offline-map-test` — тестовая страница MBTiles-рендеринга
- ✅ Кнопка «Офлайн» в Map.tsx с dropdown-меню
- ✅ Избранные метки (favorites) — API + UI

### Нужно доработать:
- 🔄 `offlineService.downloadRegionData()` — сейчас заглушки (возвращает пустые массивы)
- 🔄 API `/api/offline/region/:id/bundle` — не существует
- 🔄 Тайловый сервер для скачивания zoom 6–12 для конкретного региона
- 🔄 IndexedDB-based TileLayer для Leaflet
- 🔄 GPS-точка на офлайн-карте
- 🔄 Premium-gate для скачивания

---

## 10. Технические заметки

### Размер тайлов по zoom-уровням (один регион средней площади):

| Zoom | Тайлов | Размер | Детализация |
|---|---|---|---|
| 6 | ~4 | ~200 КБ | Общий обзор России |
| 7 | ~8 | ~400 КБ | Федеральный округ |
| 8 | ~20 | ~1 МБ | Область целиком |
| 9 | ~60 | ~3 МБ | Крупные города видны |
| 10 | ~200 | ~5 МБ | Районные центры |
| 11 | ~600 | ~10 МБ | Дороги, сёла |
| 12 | ~2000 | ~15 МБ | Улицы в городах |
| **Итого 6-12** | **~2900** | **~35 МБ** | Достаточно для ориентировки |

### Запись GPS-трека:
- Работает ТОЛЬКО при открытом приложении (foreground)
- `watchPosition` с `enableHighAccuracy: true`
- Интервал записи: каждые 5 секунд или 50 метров
- Downsample при сохранении (Douglas-Peucker) для экономии места

### Офлайн-определение:
```ts
// Комбинация navigator.onLine + проверка fetch
const isOffline = !navigator.onLine || await fetch('/api/ping').catch(() => true);
```

---

## 11. Критерии приёмки (MVP)

- [ ] Premium-пользователь скачал «Республику Алтай» → 85 регионов доступны для выбора
- [ ] Отключил интернет → видит карту региона (тайлы zoom 6–12)
- [ ] Видит свою GPS-позицию на карте (пульсирующая точка)
- [ ] Видит свои метки + избранные (★) метки на карте
- [ ] Создаёт новую метку с фото → сохранено в IndexedDB
- [ ] Включил интернет → метка автоматически синхронизирована
- [ ] Free-пользователь без скачанных карт: может создать метку по GPS-координатам, но карты не видит

---

## 12. История изменений

| Дата | Что изменилось |
|---|---|
| 2026-02-24 | Полная переработка концепции: от «показа треков» к «Карманному геоблогу» |
| 2026-02-24 | Определены zoom 6–12, freemium-модель, архитектура данных |
| 2026-02-24 | Избранные метки как основа «найти интересное» (вместо топ-N популярных) |

Если подтверждаете — приступаю к правкам по приоритету *Persist recorded tracks → API сохранения → UI + тесты*. Если хотите сначала только PR/план — укажете в ответе. 

*Файл создан автоматически: OFFLANE_ROAD.md*