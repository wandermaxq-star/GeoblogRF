# Frontend — example .env (Vite)

# Backend API endpoint
VITE_API_URL=http://localhost:3002
VITE_API_BASE_URL=http://localhost:3002
VITE_API_ORIGIN=http://localhost:3002

# WebSocket (optional)
VITE_WS_URL=ws://localhost:8080

# Map / geocoding keys (set for map features)
VITE_YANDEX_MAPS_API_KEY=your_yandex_maps_key
VITE_OPENROUTESERVICE_API_KEY=your_openrouteservice_api_key

# Feature toggles / runtime flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_ERROR_REPORTING=false
VITE_ENABLE_PWA=false

# NOTES:
# - Prefix with VITE_ for variables to be exposed to client code.
# - Keep real keys out of repo; use environment / CI for production.
