# Рефакторинг ProfilePage и навигации

## Цель

Перевести ProfilePage из модального окна (ProfilePanel) в полноценную glass-страницу с 3 темами, не затрагивая функционал избранного (он уже работает идеально).

---

## Текущее состояние

### Что работает идеально:
- ✅ **Избранное** — создано, работает на map.tsx и planner.tsx
- ✅ Стили избранного — есть для светлого и тёмного режима
- ✅ Единый глобальный store избранного
- ✅ **ProfilePanel** — модальное окно с вкладками (Профиль, Друзья, Избранное, Посты, Настройки)
- ✅ **CentrePage** — glass-стиль с 3 темами (light, dark, emerald)
- ✅ **Glass-стили** — CSS-переменные для 3 тем

### Проблемы:
- ❌ ProfilePanel открывается как модальное окно с крестиком
- ❌ ProfilePanel имеет фон карты (картографический стиль)
- ❌ ProfilePanel дублирует навигацию (Sidebar + Topbar)
- ❌ ProfilePage не существует как отдельная страница
- ❌ Маршрут `/profile` отсутствует

---

## Желаемое состояние

### Страницы проекта:

| # | Страница | Визуальный стиль | Фон | Содержимое |
|---|----------|------------------|-----|-----------|
| 1 | Карта (Leaflet) | Картографический | Карта Leaflet | Карта меток |
| 2 | Планировщик (YandexMaps) | Картографический | YandexMaps | Планирование маршрутов |
| 3 | Календарь событий | Картографический | Карта Leaflet | Календарь событий |
| 4 | Лента активности | Картографический | Карта Leaflet | Лента постов/комментариев |
| 5 | Посты | Картографический | Карта Leaflet | Создание/просмотр постов |
| 6 | **Личный кабинет** (`/profile`) | **Glass + 3 темы** | **Gradient** | Профиль, статистика, настройки |
| 7 | **Избранное** (`/favorites`) | **Glass + 3 темы** | **Gradient** | Метки, маршруты, события |
| 8 | **Центр влияния** (`/centre`) | **Glass + 3 темы** | **Gradient** | Галактика, квесты, лидерборды |
| 9 | PRO Аккаунт (`/pro`) | **Glass + 3 темы** | **Gradient** | Преимущества подписки |
| 10 | Пользовательское соглашение (`/legal/user-agreement`) | **Glass + 3 темы** | **Gradient** | Юридическая информация |
| 12 | Политика конфиденциальности (`/legal/privacy-policy`) | **Glass + 3 темы** | **Gradient** | Юридическая информация |

### Topbar (сверху):
- ✅ Кнопка личного кабинета (аватар)
- ✅ Кнопка выхода
- ✅ Колокольчик с уведомлениями:
  - Новые метки
  - Промодерированный контент
  - Новые посты
  - Обсуждения
- ✅ Переключатель стилей (☀️ → 🌙 → 🍃)

### Sidebar (слева):
- 🗺️ Карта
- 📅 Календарь
- 📰 Посты
- 👤 **Личный кабинет** → `/profile`
- ⭐ **Избранное** → `/favorites`
- 🏆 **Центр влияния** → `/centre`
- 👑 PRO Аккаунт
- 🤝 Партнёры (удаляется)

- ⚙️ Админ-панель (только для админов)
- 📄 Пользовательское соглашение
- 🔒 Политика конфиденциальности

---

## Что остаётся как есть (НЕ трогаем)

- ✅ **FavoritesContext** — глобальное хранилище избранного
- ✅ **FavoritesPanel** — компонент панели избранного (используется на map/planner)
- ✅ Стили избранного — светлый и тёмный режим работают
- ✅ Функционал "добавить в избранное" на map.tsx и planner.tsx
- ✅ **ProfilePanel** — оставляем как быстрый доступ из Topbar (упрощённый)

---

## Что нужно сделать

### Шаг 1: Создать ProfilePage

**Файл:** `frontend/src/pages/ProfilePage.tsx`

**Содержимое:**
- Desktop: glass-панель поверх анимированного gradient-фона (как CentrePage)
- Mobile: glass-страница
- Поддержка 3 тем (light, dark, emerald)
- Вкладки:
  - **Профиль** — информация о пользователе, быстрые действия
  - **Статистика** — активность, достижения
  - **Настройки** — уведомления, конфиденциальность, выход
- Быстрые действия:
  - Мои посты → `/posts`
  - Избранное → `/favorites`
  - Календарь → `/calendar`
  - Центр влияния → `/centre`

**Стили:**
- Использовать glass-классы (`centre-glass-card`, `cg-text`, и т.д.)
- CSS-переменные из `_glass-theme.css`
- Gradient-фон из `_standalone-mode.css`

---

### Шаг 2: Обновить Sidebar

**Файл:** `frontend/src/components/Sidebar.tsx`

**Изменения:**
1. Для пункта "Личный кабинет":
   - Вместо `setRightContent('profile')` → `navigate('/profile')`
   - Добавить `type: 'page'` и `path: '/profile'`

2. Убрать ProfilePanel из Sidebar:
   - Удалить `{rightContent === 'profile' && <ProfilePanel ... />}`
   - Удалить импорт `ProfilePanel`

3. Убрать функцию `handleCloseProfile` (не нужна)

---

### Шаг 3: Обновить Topbar

**Файл:** `frontend/src/components/Topbar.tsx`

**Изменения:**
1. Для кнопки "Личный кабинет" в меню пользователя:
   - Вместо `setRightContent('profile')` → `navigate('/profile')`

---

### Шаг 4: Добавить маршрут

**Файл:** `frontend/src/routes.tsx`

**Изменения:**
- Добавить маршрут: `<Route path="/profile" element={<ProfilePage />} />`

---

### Шаг 5: Упростить ProfilePanel (опционально)

**Файл:** `frontend/src/components/ProfilePanel.tsx`

**Изменения:**
1. Убрать вкладку "Избранное":
   - Оставить только счётчики (количество меток, маршрутов, событий)
   - Добавить кнопку "Открыть избранное" → `/favorites`

2. Убрать вкладку "Друзья":
   - Оставить только счётчики (количество друзей, заявок)
   - Добавить кнопку "Открыть друзей" → `/friends`

3. Оставить:
   - Вкладку "Профиль" с ProfileCenter
   - Вкладку "Посты" с черновиками
   - Вкладку "Настройки"

**Или вообще убрать ProfilePanel и использовать только ProfilePage** — на выбор.

---

## Проверки после каждого шага

### После Шага 1 (ProfilePage):
- [ ] ПрофильPage открывается по URL `/profile`
- [ ] Desktop: glass-панель поверх gradient-фона
- [ ] Mobile: glass-страница
- [ ] Переключение тем (light/dark/emerald) работает
- [ ] Вкладки переключаются корректно
- [ ] Быстрые действия переходят на правильные страницы

### После Шага 2 (Sidebar):
- [ ] Клик на "Личный кабинет" в Sidebar открывает `/profile`
- [ ] ProfilePanel не открывается из Sidebar
- [ ] ProfilePage открывается как полноценная страница (не модальное окно)
- [ ] Крестика нет

### После Шага 3 (Topbar):
- [ ] Клик на "Личный кабинет" в меню пользователя открывает `/profile`
- [ ] ProfilePanel не открывается из Topbar

### После Шага 4 (routes.tsx):
- [ ] Маршрут `/profile` работает
- [ ] Прямой переход по URL `/profile` работает

### После Шага 5 (ProfilePanel):
- [ ] ProfilePanel упрощён (если решили упростить)
- [ ] Счётчики избранного отображаются
- [ ] Кнопка "Открыть избранное" работает

---

## Принципы реализации

1. **Малой кровью** — используем то, что уже работает
2. **Не создаём ничего нового** — избранное уже работает, ProfilePanel уже есть
3. **Маленькие шаги** — реализуем по шагам с проверкой после каждого
4. **Единое состояние** — избранное остаётся глобальным store
5. **Единый стиль** — solo-pages (ProfilePage, CentrePage) используют glass + gradient

---

## Что НЕ нужно делать

- ❌ НЕ переносить функционал избранного из ProfilePanel в отдельную страницу (избранное уже работает)
- ❌ НЕ переписывать избранное с нуля
- ❌ НЕ менять работу избранного на map.tsx и planner.tsx
- ❌ НЕ менять store избранного
- ❌ НЕ менять стили избранного

---

## Что было сделано

### ✅ Создана FavoritesPage (отдельная страница)
- **Файл:** `frontend/src/pages/Favorites.tsx`
- **Тип:** Отдельная страница (solo-page) — полноэкранный режим
- **Маршрут:** `/favorites`
- **Двуслойная структура (аналогично posts.tsx):**
  - **L1 (первый слой):** `favorites-static-header` — статичный заголовок "Избранное" по центру
  - **L2 (второй слой):** `favorites-scroll-area` → `favorites-content-centered` — контент с отступами от краёв (max-width 900px, margin: 0 auto)
- **Стиль:** 
  - Использует `MirrorGradientContainer` с классом `glass-${glassVariant}` (3 темы: light/dark/emerald)
  - Контейнер: `page-layout-container page-container favorites-mode`
  - Структура: `page-main-area` → `page-content-wrapper` → `page-main-panel`
- **CSS классы:**
  - `.favorites-static-header` — L1 заголовок (аналог `.posts-static-header`)
  - `.favorites-title-row` — ряд с заголовком
  - `.favorites-main-title` — заголовок h1
  - `.favorites-scroll-area` — L2 скролльная область
  - `.favorites-content-centered` — контент по центру с max-width 900px
- **FavoritesPanel:**
  - Ширина: 100% (занимает всю ширину контейнера)
  - Заголовок скрыт (`showHeader={false}`) — используется L1 заголовок из page-main-panel
  - Интегрирован в centered контейнер с едиными отступами
- **Функционал:**
  - Использует готовый компонент **FavoritesPanel** напрямую
  - Вкладки: Метки, Маршруты, События
  - Поиск и фильтрация
  - Выбор элементов (мульти-селект)
  - Действия: Построить маршрут, Переместить в планировщик, Загрузить маршрут
  - Удаление из избранного
  - Интеграция с RoutePlannerContext
- **Переключение тем:** Работает через Topbar (useThemeStore), применяется класс `glass-light`, `glass-dark` или `glass-emerald`

### ✅ Добавлен маршрут `/favorites`
- **App.tsx:** Добавлен маршрут с lazy-загрузкой FavoritesPage
- **routes.tsx:** Добавлен маршрут `/favorites`

### ✅ Обновлена документация
- **REFACTORING_PROFILE_PAGE.md:** Обновлена информация о реализации FavoritesPage

---

## Что нужно сделать

- ✅ Создать ProfilePage с glass-стилем
- ✅ Обновить навигацию в Sidebar и Topbar
- ✅ Убрать ProfilePanel из Sidebar
- ✅ Создать FavoritesPage с использованием FavoritesPanel
- ✅ Добавить маршрут /favorites
- ✅ Растянуть FavoritesPage на весь контейнер
- ✅ Убрать белый фон и дублирующий заголовок из FavoritesPage
- ✅ Обновить документацию
- ⏳ Очистить ProfilePanel от дублирующего функционала
- ⏳ Удалить старый файл Test.tsx (больше не нужен)

---

## Резюме

**Цель:** ProfilePage и FavoritesPage — полноценные glass-страницы с 3 темами.

**Ключевой момент:**
- ProfilePage — личный кабинет (профиль, статистика, настройки) — solo-страница с абстрактным фоном
- FavoritesPage — избранное (метки, маршруты, события) — отдельная страница `/favorites`, растянута на весь контейнер
- FavoritesPage использует FavoritesPanel напрямую, без лишних обёрток
- Нет белого фона, нет дублирующего заголовка "Избранное"
- Фон карты Leaflet виден (activity-map-background)
- Автоматическая смена тем через CSS переменные (light, dark, emerald)

---

## Порядок выполнения

1. **Прочитать этот документ** — убедиться, что всё понятно
2. **Задать вопросы** — если что-то неясно
3. **Начать с Шага 1** — создать ProfilePage
4. **Проверить после Шага 1** — убедиться, что работает
5. **Перейти к Шагу 2** — обновить Sidebar
6. **Проверить после Шага 2** — убедиться, что работает
7. **Перейти к Шагу 3** — обновить Topbar
8. **Проверить после Шага 3** — убедиться, что работает
9. **Перейти к Шагу 4** — добавить маршрут
10. **Проверить после Шага 4** — убедиться, что работает
11. **Перейти к Шагу 5** — упростить ProfilePanel (опционально)
12. **Проверить после Шага 5** — убедиться, что работает
13. **Финальное тестирование** — проверить всё вместе

---

## Вопросы для уточнения

1. Нужно ли упрощать ProfilePanel или полностью убрать его?
2. Нужно ли оставлять ProfilePanel как быстрый доступ из Topbar?
3. Нужно ли добавлять счётчики избранного на ProfilePage?
4. Нужно ли добавлять счётчики друзей на ProfilePage?

---

*Документ создан для планирования рефакторинга ProfilePage. После прочтения подтвердите, что всё понятно, и начнём реализацию по шагам.*