# Центр Влияния — Дорожная карта реализации

> **Ветка:** `mobile-centre`  
> **Дата старта:** 26.02.2026  
> **Цель:** Полноценный мобильный хаб прогресса, соревнований и мотивации

---

## Концепция

Центр Влияния — **личный хаб прогресса и соревнований**, а не информационная доска.  
Фокус: «что я заработал → что могу выиграть → как я круче других».

Десктопная версия (`InfluenceCenter/`) остаётся как есть. Мобильная — отдельная страница `pages/Mobile/CentrePage.tsx` в glassmorphism-стиле (`m-glass-*` классы).

---

## Текущее состояние

### ✅ Что уже работает
| Элемент | Файл | Данные |
|---|---|---|
| Backend API геймификации | `backend/src/controllers/gamificationController.js` | 567 строк, полный CRUD |
| 6 таблиц PostgreSQL | `backend/src/migrations/create_gamification_tables.sql` | user_levels, xp_history, daily_goals, daily_goals_history, user_achievements, gamification_actions |
| API уровней и XP | `GET /api/gamification/level`, `POST /api/gamification/xp` | Реальные данные |
| API ежедневных целей | `GET /api/gamification/daily-goals` | Реальные данные (генерация — TODO) |
| API достижений | `GET /api/gamification/achievements` | Реальные данные |
| API статистики | `GET /api/gamification/stats` | Частично (recentXP, byRarity — TODO) |
| Feature Flags | `frontend/src/config/gamificationFeatures.ts` | 4 этапа по кол-ву пользователей |
| GamificationFacade | `frontend/src/services/gamificationFacade.ts` | Модерация, дубликаты, кулердауны, лимиты |
| ProgressRating | `frontend/src/components/InfluenceCenter/ProgressRating.tsx` | Реальные данные |
| GlobalGoalsWidget | `frontend/src/components/InfluenceCenter/GlobalGoalsWidget.tsx` | Реальные данные |
| TrendingPosts | `frontend/src/components/InfluenceCenter/TrendingPosts.tsx` | Реальные данные |
| Типы | `frontend/src/types/gamification.ts` | Полные TypeScript-типы |

### ⚠️ Что на моках (захардкожено)
| Элемент | Файл | Проблема |
|---|---|---|
| StatsCards | `InfluenceCenter/StatsCards.tsx` | Моковые цифры (2,847 маркеров и т.д.) |
| TopContributors | `InfluenceCenter/TopContributors.tsx` | Моковые пользователи |
| CommunityFeed | `InfluenceCenter/CommunityFeed.tsx` | Моковая лента |
| TrendsSection | `InfluenceCenter/TrendsSection.tsx` | Моковые тренды |

### ❌ Чего нет
- Мобильная страница Центра Влияния
- Сезонные конкурсы (ни бэкенд, ни фронт)
- Розыгрыши
- Лучшие маршруты с оффлайн-скачиванием
- Реальный лидерборд (API endpoint)
- Генерация ежедневных целей на бэкенде

---

## Архитектура мобильной страницы

### Секции (сверху вниз):

```
┌─────────────────────────────┐
│  1. Карточка уровня         │  ← API /gamification/level
│     Аватар · Ранг · Стрик   │
│     [═══════70%════───] L12  │
├─────────────────────────────┤
│  2. Ежедневные задания (×3) │  ← API /gamification/daily-goals
│  ☐ Добавь 1 метку    +20xp │
│  ☑ Пост с фото       +30xp │
│  ☐ Оцени 3 места     +15xp │
│  ──── бонус за все: +50xp ──│
├─────────────────────────────┤
│  3. Достижения              │  ← API /gamification/achievements
│  [🗺️] [📸] [🧭] [✍️] [🔥] │
│  горизонтальный скролл      │
├─────────────────────────────┤
│  4. Сезонный конкурс        │  ← NEW API
│  🏔️ Зимний марафон 2026    │
│  до конца: 3д 14ч           │
│  [Таблица лидеров сезона]   │
├─────────────────────────────┤
│  5. Лучшие маршруты         │  ← API /routes + рейтинг
│  [карточка] [карточка] →    │
│  📥 Скачать оффлайн         │
├─────────────────────────────┤
│  6. Лидерборд               │  ← NEW API /gamification/leaderboard
│  🥇 User1  4200xp  ↑2      │
│  🥈 User2  3800xp  ↓1      │
│  🥉 User3  3100xp  ↑5      │
│  ...                        │
│  ── Вы: #42  890xp  ↑3 ──  │
├─────────────────────────────┤
│  7. Розыгрыши               │  ← NEW API
│  🎁 Лучший пользователь    │
│  Условие: 500xp за неделю  │
│  [Участвовать]              │
└─────────────────────────────┘
```

---

## Фазы реализации

### Фаза 1 — Базовая мобильная страница (MVP)

**Приоритет:** 🔴 Высокий  
**Оценка:** 1 рабочий день

#### Frontend

| # | Задача | Файл | Описание |
|---|---|---|---|
| 1.1 | Создать мобильную страницу | `pages/Mobile/CentrePage.tsx` | Контейнер с glassmorphism, scroll-контейнер |
| 1.2 | Карточка уровня | `components/Mobile/Centre/LevelCard.tsx` | Круговой прогресс-бар, ранг, стрик, аватар. API: `/gamification/level` |
| 1.3 | Ежедневные задания | `components/Mobile/Centre/DailyGoals.tsx` | 3 задания с чекбоксами и прогресс-барами. API: `/gamification/daily-goals` |
| 1.4 | Достижения (горизонтальный скролл) | `components/Mobile/Centre/AchievementsRow.tsx` | Круглые иконки по категориям, незаработанные — размыты. API: `/gamification/achievements` |
| 1.5 | Подключить роутинг | `routes.tsx` | Мобильная версия `/centre` → `CentrePage` |

#### Backend

| # | Задача | Файл | Описание |
|---|---|---|---|
| 1.6 | Генерация ежедневных целей | `gamificationController.js` | Заменить TODO-заглушку, генерировать 3 цели на день (create_posts, create_markers, add_photos) |
| 1.7 | Заполнить stats endpoint | `gamificationController.js` | Вернуть реальные `recentXP` и `byRarity` |

---

### Фаза 2 — Лидерборд и лучшие маршруты

**Приоритет:** 🟡 Средний  
**Оценка:** 1 рабочий день

#### Backend

| # | Задача | Файл | Описание |
|---|---|---|---|
| 2.1 | Endpoint лидерборда | `gamificationController.js` | `GET /api/gamification/leaderboard?period=week\|month\|season\|all` → `SELECT FROM user_levels ORDER BY total_xp DESC LIMIT 10` + позиция текущего пользователя |
| 2.2 | Endpoint лучших маршрутов | `src/routes/routes.js` | `GET /api/routes/top?period=week\|month` → сортировка по рейтингу/лайкам, LIMIT 5 |

#### Frontend

| # | Задача | Файл | Описание |
|---|---|---|---|
| 2.3 | Компонент лидерборда | `components/Mobile/Centre/Leaderboard.tsx` | Табы (Неделя/Месяц/Сезон/Всё время), топ-10, своя позиция внизу. Топ-3 — увеличенные карточки с 🥇🥈🥉 |
| 2.4 | Лучшие маршруты | `components/Mobile/Centre/TopRoutes.tsx` | Горизонтальный скролл карточек, название/автор/длина/рейтинг, кнопка «📥 Скачать оффлайн» |
| 2.5 | Интеграция с оффлайн | — | При клике «Скачать оффлайн» — открывать `OfflineMapBottomSheet` с предзаполненным маршрутом |

---

### Фаза 3 — Сезонные конкурсы

**Приоритет:** 🟡 Средний  
**Оценка:** 1–2 рабочих дня

#### Концепция сезонов

| Сезон | Период | Тематика | Пример цели |
|---|---|---|---|
| ❄️ Зимний | Декабрь — Февраль | Горнолыжные курорты, зимние виды | «Отметь 10 зимних мест» |
| 🌸 Весенний | Март — Май | Парки, сады, пробуждение природы | «Создай 5 маршрутов по паркам» |
| ☀️ Летний | Июнь — Август | Пляжи, походы, путешествия | «Запиши 3 GPS-трека» |
| 🍂 Осенний | Сентябрь — Ноябрь | Лесные маршруты, урожай, культура | «Напиши 5 блогов о путешествиях» |

#### Database

| # | Задача | Описание |
|---|---|---|
| 3.1 | Таблица `seasons` | `id`, `name`, `theme`, `start_date`, `end_date`, `goals` (JSONB), `rewards` (JSONB), `is_active` |
| 3.2 | Таблица `season_participants` | `user_id`, `season_id`, `xp_earned`, `goals_completed` (JSONB), `joined_at` |
| 3.3 | Таблица `season_achievements` | `id`, `season_id`, `title`, `icon`, `condition` (JSONB), `rarity` — уникальные бейджи сезона |

#### Backend

| # | Задача | Описание |
|---|---|---|
| 3.4 | `GET /api/seasons/current` | Текущий активный сезон с целями и наградами |
| 3.5 | `POST /api/seasons/:id/join` | Присоединиться к сезону |
| 3.6 | `GET /api/seasons/:id/leaderboard` | Топ участников сезона |
| 3.7 | `GET /api/seasons/:id/achievements` | Сезонные достижения пользователя |

#### Frontend

| # | Задача | Файл | Описание |
|---|---|---|---|
| 3.8 | Баннер сезона | `components/Mobile/Centre/SeasonBanner.tsx` | Тематический баннер, обратный отсчёт, кнопка «Участвовать» |
| 3.9 | Сезонные достижения | `components/Mobile/Centre/SeasonAchievements.tsx` | Уникальные бейджи (после окончания сезона — больше не получить) |
| 3.10 | Сезонный лидерборд | Использовать `Leaderboard.tsx` | Добавить таб «Сезон» с данными из `/seasons/:id/leaderboard` |

---

### Фаза 4 — Розыгрыши и «Лучший пользователь»

**Приоритет:** 🔵 Низкий  
**Оценка:** 1–2 рабочих дня

#### Концепция

- **Лучший пользователь месяца:** Автоматически — топ-1 по XP за месяц. Получает:
  - Бейдж «⭐ Лучший — Февраль 2026» (навсегда в профиле)
  - Золотая рамка аватара на месяц
  - Доступ к бета-функциям

- **Розыгрыши:** Ручные (создаются админом). Условие участия — набрать N XP за период. Примеры:
  - «Набери 500 XP за неделю — участвуй в розыгрыше»
  - Приз: особый бейдж, расширенные лимиты загрузки, эксклюзивная тема оформления

#### Database

| # | Задача | Описание |
|---|---|---|
| 4.1 | Таблица `giveaways` | `id`, `title`, `description`, `prize` (JSONB), `condition_type`, `condition_value`, `start_date`, `end_date`, `winner_id`, `is_active` |
| 4.2 | Таблица `giveaway_participants` | `user_id`, `giveaway_id`, `qualified`, `joined_at` |
| 4.3 | Таблица `user_badges` | `user_id`, `badge_id`, `title`, `icon`, `type` (seasonal/monthly/giveaway), `earned_at`, `expires_at` |

#### Backend

| # | Задача | Описание |
|---|---|---|
| 4.4 | `GET /api/giveaways/active` | Текущие розыгрыши |
| 4.5 | `POST /api/giveaways/:id/join` | Участвовать (проверка условий) |
| 4.6 | `GET /api/users/:id/badges` | Все бейджи пользователя |
| 4.7 | Cron: определение лучшего пользователя месяца | 1-го числа каждого месяца → auto-badge + уведомление |

#### Frontend

| # | Задача | Файл | Описание |
|---|---|---|---|
| 4.8 | Карточка розыгрыша | `components/Mobile/Centre/GiveawayCard.tsx` | Приз, условие, обратный отсчёт, кнопка «Участвовать» |
| 4.9 | Бейдж «Лучший пользователь» в профиле | `pages/Mobile/ProfilePage.tsx` | Золотая рамка + бейдж рядом с аватаром |
| 4.10 | Витрина бейджей | `components/Mobile/Centre/BadgeShowcase.tsx` | Все заработанные бейджи (сезонные, monthly, розыгрыши) |

---

## Достижения — полный список

### Категория: 🗺️ Исследователь (places)

| ID | Название | Условие | Рарность | XP |
|---|---|---|---|---|
| `explorer_10` | Первые шаги | 10 маркеров | Common | 50 |
| `explorer_50` | Картограф | 50 маркеров | Rare | 150 |
| `explorer_100` | Мастер карт | 100 маркеров | Epic | 300 |
| `explorer_500` | Легенда карт | 500 маркеров | Legendary | 1000 |

### Категория: 📸 Фотограф (posts)

| ID | Название | Условие | Рарность | XP |
|---|---|---|---|---|
| `photo_5` | Фотолюбитель | 5 постов с фото | Common | 50 |
| `photo_20` | Фотограф | 20 постов с фото | Rare | 150 |
| `photo_100` | Фотомастер | 100 постов с фото | Epic | 300 |

### Категория: 🧭 Штурман (routes)

| ID | Название | Условие | Рарность | XP |
|---|---|---|---|---|
| `route_5` | Навигатор | 5 маршрутов | Common | 50 |
| `route_20` | Штурман | 20 маршрутов | Rare | 150 |
| `route_50` | Мастер маршрутов | 50 маршрутов | Epic | 300 |

### Категория: ✍️ Блогер (content)

| ID | Название | Условие | Рарность | XP |
|---|---|---|---|---|
| `blog_5` | Начинающий автор | 5 блогов | Common | 50 |
| `blog_20` | Блогер | 20 блогов | Rare | 150 |
| `comment_50` | Комментатор | 50 комментариев | Rare | 100 |

### Категория: 🔥 Активность (activity)

| ID | Название | Условие | Рарность | XP |
|---|---|---|---|---|
| `streak_7` | Неделя огня | 7 дней стрик | Common | 100 |
| `streak_30` | Месяц огня | 30 дней стрик | Rare | 300 |
| `streak_100` | Огненная легенда | 100 дней стрик | Legendary | 1000 |
| `daily_allcomplete_10` | Трудоголик | 10 раз выполнить все дневные задания | Rare | 200 |

### Категория: 📡 Оффлайнер (offline)

| ID | Название | Условие | Рарность | XP |
|---|---|---|---|---|
| `offline_first` | Автономный | Первая скачанная карта | Common | 30 |
| `offline_5routes` | Без связи | 5 маршрутов скачано оффлайн | Rare | 100 |
| `gps_track_first` | GPS-следопыт | Первый записанный GPS-трек | Common | 50 |
| `gps_track_long` | Марафонец | GPS-трек > 50 км | Epic | 300 |

### Категория: ⭐ Специальные (special)

| ID | Название | Условие | Рарность | XP |
|---|---|---|---|---|
| `best_user_month` | Лучший пользователь | Топ-1 за месяц | Legendary | 500 |
| `best_route_week` | Маршрут недели | Маршрут попал в «Лучшие» | Epic | 200 |
| `season_*` | Сезонные бейджи | Выполнить цели сезона | Epic/Legendary | 300–500 |

---

## Ранговая система

| Уровни | Ранг | Эмодзи | Привилегии |
|---|---|---|---|
| 1–5 | Новичок | 🌱 | Базовый функционал |
| 6–15 | Исследователь | 🧭 | Расширенные лимиты загрузки фото |
| 16–30 | Путешественник | 🗺️ | Создание событий, оффлайн-карты |
| 31–49 | Легенда | ⚡ | Модерация комментариев, бета-функции |
| 50+ | ГеоБлогер | 👑 | Всё + золотая рамка + приоритетная поддержка |

**Формула XP:** $XP_{required} = 100 \cdot level^{1.5}$

---

## Структура файлов (итоговая)

```
frontend/src/
├── pages/Mobile/
│   └── CentrePage.tsx                    ← Фаза 1
├── components/Mobile/Centre/
│   ├── LevelCard.tsx                     ← Фаза 1 (карточка уровня)
│   ├── DailyGoals.tsx                    ← Фаза 1 (ежедневные задания)
│   ├── AchievementsRow.tsx               ← Фаза 1 (горизонтальный скролл)
│   ├── Leaderboard.tsx                   ← Фаза 2 (рейтинг)
│   ├── TopRoutes.tsx                     ← Фаза 2 (лучшие маршруты)
│   ├── SeasonBanner.tsx                  ← Фаза 3 (сезонный баннер)
│   ├── SeasonAchievements.tsx            ← Фаза 3 (сезонные достижения)
│   ├── GiveawayCard.tsx                  ← Фаза 4 (розыгрыши)
│   └── BadgeShowcase.tsx                 ← Фаза 4 (витрина бейджей)
│
backend/src/
├── controllers/
│   ├── gamificationController.js         ← Доработка Фаза 1–2
│   ├── seasonController.js               ← Фаза 3
│   └── giveawayController.js             ← Фаза 4
├── routes/
│   ├── gamificationRoutes.js             ← Доработка Фаза 2
│   ├── seasonRoutes.js                   ← Фаза 3
│   └── giveawayRoutes.js                ← Фаза 4
├── migrations/
│   ├── create_seasons_tables.sql         ← Фаза 3
│   └── create_giveaways_tables.sql       ← Фаза 4
```

---

## Что НЕ переносить в мобильную версию

| Компонент | Причина |
|---|---|
| `CommunityHeader` (Hero-баннер) | «Стань частью сообщества» — не нужен залогиненному пользователю |
| `StatsCards` (2,847 маркеров) | Абстрактные числа сообщества не мотивируют |
| `CommunityFeed` | Дублирует ленту постов |
| `TrendsSection` (тренды, теги) | Для аналитической страницы, не для мотивационного хаба |
| `TopContributors` (в текущем виде) | Моковые данные — заменить на реальный `Leaderboard` |

---

## Визуальный стиль

- **Glassmorphism** — используем существующие `m-glass-*` классы из `_mobile-glass.css`
- **Уровень/ранг** — круговой SVG прогресс-бар с градиентом (цвет зависит от ранга)
- **Достижения** — круглые иконки 56×56px
  - Заработанные: яркие с подсветкой рарности (Common=серый, Rare=синий, Epic=фиолетовый, Legendary=золотой + анимация пульса)
  - Не заработанные: blur + серый силуэт + подсказка при тапе
- **Сезонный баннер** — gradient-фон по тематике (зима=сине-белый, лето=оранжево-жёлтый)
- **Лидерборд** — топ-3 с увеличенными карточками, 🥇🥈🥉, остальные — компактный список
- **Розыгрыши** — карточка с shimmer-эффектом на призе

---

## Зависимости

```
Фаза 1 (MVP)
  └── ничего, всё API уже есть

Фаза 2 (Лидерборд + Маршруты)
  ├── Фаза 1
  └── новый endpoint leaderboard

Фаза 3 (Сезоны)
  ├── Фаза 2 (лидерборд → сезонный лидерборд)
  └── новые таблицы + контроллер

Фаза 4 (Розыгрыши)
  ├── Фаза 2 (лидерборд для определения winner)
  └── новые таблицы + контроллер + cron
```

---

## Чеклист запуска

- [ ] Создать ветку `mobile-centre`
- [ ] **Фаза 1:** CentrePage + LevelCard + DailyGoals + AchievementsRow
- [ ] **Фаза 1:** Доработать бэкенд (генерация целей, stats)
- [ ] **Фаза 2:** Leaderboard endpoint + компонент
- [ ] **Фаза 2:** TopRoutes + интеграция с оффлайн
- [ ] **Фаза 3:** Таблицы сезонов + SeasonBanner + SeasonAchievements
- [ ] **Фаза 4:** Giveaways + BadgeShowcase + best user cron
- [ ] Тестирование всех секций
- [ ] Merge в `main`
